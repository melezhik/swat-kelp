export no_proxy=127.0.0.1
swat_purge_cache=0
try_num=2
if perl -MNet::EmptyPort -e 1 2>/dev/null; then
    bind_port=${bind_port:=`perl -MNet::EmptyPort -e 'print Net::EmptyPort::empty_port()'`}
else
    bind_port=5000
fi
port=$bind_port

